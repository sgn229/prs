--- a/src/socks5.c
+++ b/src/socks5.c
@@ -1293,7 +1293,3 @@
     if (srv->dns_using_cares) {
-        char *servers = ares_get_servers_csv(srv->dns_channel);
-        if (servers) {
-            fprintf(stderr, "SOCKS5 DNS servers: %s\n", servers);
-            ares_free_string(servers);
-        }
+        fprintf(stderr, "SOCKS5 DNS servers configured via c-ares\n");
     } else {
